{
    "type": "AdaptiveCard",
    "$schema": "http://adaptivecards.io/schemas/adaptive-card.json",
    "version": "1.3",
    "body": [
        {
            "type": "TextBlock",
            "text": "${exchange}",
            "wrap": true
        },
        {
            "type": "Image",
            "url": "${equityChart}"
        }
    ]
}
